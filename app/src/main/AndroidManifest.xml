<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uielements2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UIElements2">
        <activity android:name=".AddAlbumAct"></activity>
        <activity android:name=".AddSongToAlbumAct" />
        <activity android:name=".NewAlbumDetailsActivity" />
        <activity android:name=".EditAlbumActivity" />
        <activity
            android:name=".AddAlbumActivity"
            android:label="@string/title_activity_add_album"
            android:theme="@style/Theme.UIElements2.NoActionBar" />
        <activity android:name=".EditSongActivity" />
        <activity android:name=".CreateSongActivity" />
        <activity android:name=".AlbumDetailsActivity" />
        <activity android:name=".AlbumActivity" />
        <activity android:name=".QueueActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>